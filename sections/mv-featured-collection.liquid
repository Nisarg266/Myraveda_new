<div class="mv-collection-section" id="mv-collection-{{ section.id }}" style="--col-heading-desktop: {{ section.settings.heading_size_desktop | default: 34 }}px; --col-heading-mobile: {{ section.settings.heading_size_mobile | default: 24 }}px; --col-prod-title-desktop: {{ section.settings.product_title_size_desktop | default: 17 }}px; --col-prod-title-mobile: {{ section.settings.product_title_size_mobile | default: 16 }}px; --col-price-desktop: {{ section.settings.price_size_desktop | default: 18 }}px; --col-price-mobile: {{ section.settings.price_size_mobile | default: 16 }}px; --col-grid-desktop: {{ section.settings.columns_desktop | default: 4 }}; --col-pt: {{ section.settings.padding_top | default: 52 }}px; --col-pb: {{ section.settings.padding_bottom | default: 52 }}px; --col-bg: {{ section.settings.background_color | default: '#FAF8F5' }};">
  <div class="mv-container">
    <div class="mv-section-header">
      {%- if section.settings.badge != blank -%}
        <span class="mv-section-badge">{{ section.settings.badge | escape }}</span>
      {%- endif -%}
      <h2 class="mv-section-title">{{ section.settings.heading | escape }}</h2>
      {%- if section.settings.subheading != blank -%}
        <p class="mv-section-subtitle">{{ section.settings.subheading | escape }}</p>
      {%- endif -%}
      <div class="mv-divider-accent"></div>
    </div>

    {%- assign target_collection = collections[section.settings.collection] -%}

    <div class="mv-product-grid">
      {%- if target_collection != blank and target_collection.products_count > 0 -%}
        {%- for product in target_collection.products limit: section.settings.products_to_show -%}
          <div class="mv-product-card">
            <a href="{{ product.url }}" class="mv-product-media-wrap">
              {%- if product.featured_media != blank -%}
                <img
                  src="{{ product.featured_media | image_url: width: 600 }}"
                  srcset="{{ product.featured_media | image_url: width: 350 }} 350w, {{ product.featured_media | image_url: width: 600 }} 600w"
                  sizes="(max-width: 749px) 50vw, (max-width: 1024px) 33vw, 280px"
                  alt="{{ product.title | escape }}"
                  loading="lazy"
                  class="mv-product-img primary"
                >
                {%- if product.media[1] != blank -%}
                  <img
                    src="{{ product.media[1] | image_url: width: 600 }}"
                    alt="{{ product.title | escape }}"
                    loading="lazy"
                    class="mv-product-img secondary"
                  >
                {%- endif -%}
              {%- else -%}
                {{ 'product-1' | placeholder_svg_tag: 'mv-product-placeholder' }}
              {%- endif -%}

              {%- if product.compare_at_price > product.price -%}
                <span class="mv-product-badge mv-badge-sale">✦ SALE</span>
              {%- else -%}
                <span class="mv-product-badge mv-badge-best">✦ BESTSELLER</span>
              {%- endif -%}
            </a>

            <div class="mv-product-info">
              <div class="mv-prod-meta-row">
                <span class="mv-prod-tag">Ayurvedic Luxury</span>
                <div class="mv-rating-stars">
                  <span>★★★★★</span>
                  <span class="mv-rating-num">4.9</span>
                </div>
              </div>

              <h3 class="mv-product-title">
                <a href="{{ product.url }}">{{ product.title | escape }}</a>
              </h3>

              <div class="mv-product-price">
                <span class="mv-current-price">{{ product.price | money }}</span>
                {%- if product.compare_at_price > product.price -%}
                  <span class="mv-compare-price">{{ product.compare_at_price | money }}</span>
                  {%- assign save_pct = product.compare_at_price | minus: product.price | times: 100.0 | divided_by: product.compare_at_price | round -%}
                  <span class="mv-save-pill">Save {{ save_pct }}%</span>
                {%- endif -%}
              </div>

              <div class="mv-product-action">
                {%- if product.available -%}
                  <form method="post" action="/cart/add">
                    <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
                    <button type="submit" class="mv-btn-add">
                      <span>Add to Bag</span>
                      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2"><path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path><line x1="3" y1="6" x2="21" y2="6"></line><path d="M16 10a4 4 0 0 1-8 0"></path></svg>
                    </button>
                  </form>
                {%- else -%}
                  <button class="mv-btn-add mv-btn-soldout" disabled>Sold Out</button>
                {%- endif -%}
              </div>
            </div>
          </div>
        {%- endfor -%}
      {%- else -%}
        {%- assign demo_titles = "Milk Therapy Hair Oil with Lavender & Rosemary,Pure Steam-Distilled Kannauj Rose Water,The Greek Goddess Shea & Zinc Scrub Mask,Japanese Matcha Tea & Camellia Shampoo" | split: "," -%}
        {%- assign demo_prices = "₹899.00,₹539.00,₹599.00,₹325.00" | split: "," -%}
        {%- for i in (0..3) -%}
          <div class="mv-product-card">
            <div class="mv-product-media-wrap">
              {{ 'product-' | append: forloop.index | placeholder_svg_tag: 'mv-product-placeholder' }}
              <span class="mv-product-badge mv-badge-best">✦ BESTSELLER</span>
            </div>
            <div class="mv-product-info">
              <div class="mv-prod-meta-row">
                <span class="mv-prod-tag">Ayurvedic Luxury</span>
                <div class="mv-rating-stars">
                  <span>★★★★★</span>
                  <span class="mv-rating-num">5.0</span>
                </div>
              </div>
              <h3 class="mv-product-title">
                <a href="#">{{ demo_titles[i] }}</a>
              </h3>
              <div class="mv-product-price">
                <span class="mv-current-price">{{ demo_prices[i] }}</span>
              </div>
              <div class="mv-product-action">
                <a href="/collections/all" class="mv-btn-add">Explore Formula</a>
              </div>
            </div>
          </div>
        {%- endfor -%}
      {%- endif -%}
    </div>

    {%- if section.settings.show_view_all -%}
      <div class="mv-collection-footer">
        <a href="{{ target_collection.url | default: '/collections/all' }}" class="mv-btn-gold">
          <span>Explore All Best-Sellers</span>
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M5 12h14"></path><path d="M12 5l7 7-7 7"></path></svg>
        </a>
      </div>
    {%- endif -%}
  </div>
</div>

<style>
  #mv-collection-{{ section.id }} {
    padding-top: var(--col-pt, clamp(42px, 5vw, 62px));
    padding-bottom: var(--col-pb, clamp(42px, 5vw, 62px));
    padding-left: 24px;
    padding-right: 24px;
    background: var(--col-bg, #FAF8F5);
  }

  #mv-collection-{{ section.id }} .mv-container {
    max-width: 1440px;
    margin: 0 auto;
    padding: 0 clamp(16px, 3.2vw, 40px);
    box-sizing: border-box;
  }

  #mv-collection-{{ section.id }} .mv-section-title {
    font-size: var(--col-heading-desktop, clamp(2rem, 3.5vw, 2.7rem));
  }

  .mv-product-grid {
    display: grid;
    grid-template-columns: repeat(var(--col-grid-desktop, 4), 1fr);
    gap: 28px;
    max-width: 1280px;
    margin: 0 auto;
    width: 100%;
    box-sizing: border-box;
  }

  .mv-product-card {
    background: #FFFFFF;
    border-radius: 18px;
    overflow: hidden;
    border: 1.5px solid rgba(201, 158, 62, 0.30);
    box-shadow: 0 6px 18px rgba(14, 40, 31, 0.04);
    display: flex;
    flex-direction: column;
    transition: all 0.38s cubic-bezier(0.16, 1, 0.3, 1);
    position: relative;
    box-sizing: border-box;
  }

  .mv-product-card:hover {
    transform: translateY(-6px);
    box-shadow: 0 18px 40px rgba(201, 158, 62, 0.20), 0 0 0 1px #C99E3E;
    border-color: #C99E3E;
  }

  .mv-product-media-wrap {
    position: relative;
    width: 100%;
    height: 280px;
    min-height: 260px;
    max-height: 300px;
    overflow: hidden;
    background: #FFFFFF;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 22px 18px 12px;
    border-bottom: 1px solid rgba(201, 158, 62, 0.16);
    text-decoration: none;
    box-sizing: border-box;
  }

  .mv-product-img {
    width: 100%;
    height: 100%;
    max-width: 100%;
    max-height: 100%;
    object-fit: contain;
    object-position: center;
    transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1), filter 0.4s ease;
    display: block;
    margin: 0 auto;
  }

  .mv-product-card:hover .mv-product-img.primary {
    transform: scale(1.04);
  }

  .mv-product-img.secondary {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    max-width: 100%;
    max-height: 100%;
    object-fit: contain;
    object-position: center;
    padding: 22px 18px 12px;
    box-sizing: border-box;
    margin: 0 auto;
    opacity: 0;
    transition: opacity 0.5s ease, transform 0.5s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .mv-product-card:hover .mv-product-img.secondary {
    opacity: 1;
    transform: scale(1.04);
  }

  .mv-product-placeholder {
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .mv-product-badge {
    position: absolute;
    top: 14px;
    left: 14px;
    font-family: 'Cinzel', serif;
    font-size: 0.66rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    padding: 5px 14px;
    border-radius: var(--mv-radius-pill);
    z-index: 2;
    text-transform: uppercase;
  }

  .mv-badge-best {
    background: linear-gradient(135deg, #0E281F 0%, #163E30 100%);
    color: #F5E8C7;
    border: 1px solid #C99E3E;
    box-shadow: 0 4px 14px rgba(14, 40, 31, 0.35);
  }

  .mv-badge-sale {
    background: linear-gradient(135deg, #DFB76C 0%, #C59B45 42%, #9E7429 100%);
    color: #FFFFFF;
    border: 1px solid #DFB76C;
    box-shadow: 0 4px 14px rgba(201, 158, 62, 0.4);
  }

  .mv-product-info {
    padding: 13px 15px 13px;
    display: flex;
    flex-direction: column;
    flex-grow: 1;
    background: transparent;
  }

  .mv-prod-meta-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 4px;
  }

  .mv-prod-tag {
    font-family: 'Cinzel', serif;
    font-size: 0.62rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    color: #8C651E;
    text-transform: uppercase;
  }

  .mv-rating-stars {
    color: #C99E3E;
    font-size: 0.82rem;
    display: inline-flex;
    align-items: center;
    gap: 3px;
  }

  .mv-rating-num {
    font-size: 0.74rem;
    color: #0E281F;
    font-family: var(--mv-font-sans);
    font-weight: 700;
  }

  .mv-product-title {
    font-family: 'Marcellus', 'Playfair Display', Georgia, serif;
    font-size: var(--col-prod-title-desktop, 1.05rem);
    line-height: 1.32;
    margin: 2px 0 6px;
    font-weight: 500;
    min-height: 2.64em;
  }

  .mv-product-title a {
    color: #0E281F;
    text-decoration: none;
    transition: color 0.25s ease;
  }

  .mv-product-title a:hover {
    color: #C99E3E;
  }

  .mv-product-price {
    margin-bottom: 9px;
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 6px;
  }

  .mv-current-price {
    font-family: var(--mv-font-sans);
    font-size: var(--col-price-desktop, 1.15rem);
    font-weight: 800;
    color: #0E281F;
    letter-spacing: -0.01em;
  }

  .mv-compare-price {
    font-family: var(--mv-font-sans);
    font-size: 0.86rem;
    color: #9C958D;
    text-decoration: line-through;
  }

  .mv-save-pill {
    font-family: var(--mv-font-sans);
    font-size: 0.64rem;
    font-weight: 700;
    color: #8C651E;
    background: rgba(201, 158, 62, 0.14);
    border: 1px solid rgba(201, 158, 62, 0.35);
    padding: 2px 7px;
    border-radius: var(--mv-radius-pill);
    letter-spacing: 0.04em;
  }

  .mv-product-action {
    margin-top: auto;
  }

  .mv-btn-add {
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 7px;
    padding: 9px 12px;
    background: #0E281F;
    border: 1.5px solid #C99E3E;
    border-radius: var(--mv-radius-pill);
    color: #FFFFFF;
    font-family: 'Cinzel', serif;
    font-size: 0.70rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    cursor: pointer;
    transition: all 0.35s cubic-bezier(0.16, 1, 0.3, 1);
    text-decoration: none;
    box-shadow: 0 4px 12px rgba(14, 40, 31, 0.18);
  }

  .mv-btn-add:hover {
    background: var(--mv-gold-gradient);
    color: #0E281F;
    border-color: #DFB76C;
    box-shadow: 0 8px 24px rgba(201, 158, 62, 0.45);
    transform: translateY(-2px);
  }

  .mv-btn-soldout {
    background: #EFECE6;
    color: #A0988F;
    border-color: #DCD6CD;
    cursor: not-allowed;
    box-shadow: none;
  }

  .mv-btn-soldout:hover {
    background: #EFECE6;
    color: #A0988F;
    border-color: #DCD6CD;
    transform: none;
    box-shadow: none;
  }

  .mv-collection-footer {
    margin-top: 36px;
    text-align: center;
  }

  @media (max-width: 1024px) {
    .mv-product-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
    }
  }

  @media (max-width: 640px) {
    #mv-collection-{{ section.id }} {
      padding-top: var(--col-pt, 34px);
      padding-bottom: var(--col-pb, 34px);
      padding-left: 18px;
      padding-right: 18px;
    }
    #mv-collection-{{ section.id }} .mv-section-title {
      font-size: var(--col-heading-mobile, 1.85rem);
    }
    .mv-product-grid {
      grid-template-columns: repeat({{ section.settings.columns_mobile | default: 2 }}, 1fr);
      gap: 12px;
    }
    .mv-product-media-wrap {
      height: 185px;
      min-height: 175px;
      padding: 12px 10px 8px;
      background: #FFFFFF;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      overflow: hidden;
    }
    .mv-product-img {
      max-height: 100%;
      height: 100%;
      width: 100%;
      object-fit: contain;
      object-position: center bottom;
      display: block;
      margin: 0 auto;
    }
    .mv-product-badge {
      top: 8px;
      left: 8px;
      font-size: 0.50rem;
      font-weight: 700;
      letter-spacing: 0.08em;
      padding: 2.5px 7.5px;
      border-radius: 999px;
      line-height: 1.15;
      box-shadow: 0 2px 6px rgba(14, 40, 31, 0.22);
    }
    .mv-product-info {
      padding: 14px 12px;
    }
    .mv-product-title {
      font-size: var(--col-prod-title-mobile, 1.02rem);
      min-height: auto;
    }
    .mv-current-price {
      font-size: var(--col-price-mobile, 1.02rem);
    }
    .mv-btn-add {
      padding: 10px 10px;
      font-size: 0.68rem;
    }
  }
</style>

{% schema %}
{
  "name": "MV Featured Collection",
  "tag": "section",
  "class": "mv-featured-collection-section",
  "settings": [
    {
      "type": "header",
      "content": "Section Header"
    },
    {
      "type": "text",
      "id": "badge",
      "label": "Badge Text",
      "default": "Best Sellers"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Customer Favorites"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Our best-sellers are categorized by high demands and stellar reviews."
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "min": 2,
      "max": 12,
      "step": 2,
      "label": "Number of Products",
      "default": 4
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "label": "Show 'View All' Button",
      "default": true
    },
    {
      "type": "header",
      "content": "Grid Columns"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 2,
      "max": 5,
      "step": 1,
      "label": "Desktop Columns",
      "default": 4
    },
    {
      "type": "select",
      "id": "columns_mobile",
      "label": "Mobile Columns",
      "options": [
        { "value": "1", "label": "1 Column" },
        { "value": "2", "label": "2 Columns" }
      ],
      "default": "2"
    },
    {
      "type": "header",
      "content": "Typography (Desktop & Mobile)"
    },
    {
      "type": "range",
      "id": "heading_size_desktop",
      "min": 22,
      "max": 52,
      "step": 1,
      "unit": "px",
      "label": "Heading Font Size (Desktop)",
      "default": 34
    },
    {
      "type": "range",
      "id": "heading_size_mobile",
      "min": 18,
      "max": 36,
      "step": 1,
      "unit": "px",
      "label": "Heading Font Size (Mobile)",
      "default": 24
    },
    {
      "type": "range",
      "id": "product_title_size_desktop",
      "min": 14,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Product Title Size (Desktop)",
      "default": 17
    },
    {
      "type": "range",
      "id": "product_title_size_mobile",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Product Title Size (Mobile)",
      "default": 16
    },
    {
      "type": "range",
      "id": "price_size_desktop",
      "min": 14,
      "max": 26,
      "step": 1,
      "unit": "px",
      "label": "Price Font Size (Desktop)",
      "default": 18
    },
    {
      "type": "range",
      "id": "price_size_mobile",
      "min": 12,
      "max": 22,
      "step": 1,
      "unit": "px",
      "label": "Price Font Size (Mobile)",
      "default": 16
    },
    {
      "type": "header",
      "content": "Section Spacing & Background"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 20,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 52
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 20,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 52
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#FAF8F5"
    }
  ],
  "presets": [
    {
      "name": "MV Featured Collection"
    }
  ]
}
{% endschema %}
